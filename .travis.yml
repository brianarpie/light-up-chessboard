language: node_js

before_script:
  - npm install karma
  - npm install bower
  - npm install karma-phantomjs-launcher
  - npm install karma-ng-html2js-preprocessor
  # TODO: resolve ng-draggable dependency issue and
  #   stop pushing bower components to repo
  # - node_modules/bower/bin/bower install

script:
  - node_modules/karma/bin/karma start karma.conf.js --single-run --browsers PhantomJS
